scikit-learn>=1.3
pandas>=2.0
anthropic>=0.34  # only needed if ANTHROPIC_API_KEY is set; pipeline runs without it
google-genai>=1.0  # only needed if GEMINI_API_KEY is set; pipeline runs without it (google-generativeai is deprecated -- do not use)
python-dotenv  # loads .env locally; never commit .env itself (see .gitignore)